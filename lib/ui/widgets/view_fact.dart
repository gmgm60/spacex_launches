import 'package:flutter/material.dart';

class ViewFact extends StatelessWidget {
  const ViewFact({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
