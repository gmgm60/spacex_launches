import 'package:flutter/material.dart';

class AddFact extends StatelessWidget {
  const AddFact({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}
